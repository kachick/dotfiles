# GitHub Configurations

Existing GitHub Workflows (`.github/workflows/*.yml`) mainly use the `.yml` extension to preserve compatibility with:
- External badge URLs
- Static file references in documentation or other repositories
- Continuity of execution history

Newer files or other configurations (e.g., `dependabot.yaml`, `action.yaml`) may use `.yaml`. Mixed extensions are allowed, and we are gradually transitioning where appropriate.
