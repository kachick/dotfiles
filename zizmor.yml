rules:
  unpinned-uses:
    config:
      policies:
        kachick/*: any
        actions/*: any
        crate-ci/*: any
  template-injection:
    ignore:
      - container.yml
      - cleanup-staging-packages.yml
